-- Cooper's Neovim Config
if not pcall(require,'options') then print("options were not loaded") end
if not pcall(require,'keymaps') then print("keymaps were not loaded") end
if not pcall(require,'packer_setup') then print("packer was not loaded") end

